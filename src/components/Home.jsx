import React from 'react'
import { Typography } from '@material-ui/core'

const Home = (props) => {
    // console.log("props from Home", props)

    return (
        <Typography>
            Home
        </Typography>
    )
}

export default Home